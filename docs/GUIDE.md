# 📖 Повна інструкція по використанню

## Зміст

1. [Швидкий старт](#швидкий-старт)
2. [Покрокова інструкція запуску](#покрокова-інструкція-запуску)
3. [Тестування автентифікації](#тестування-автентифікації)
4. [API Endpoints](#api-endpoints)
5. [Налаштування](#налаштування)
6. [Вирішення проблем](#вирішення-проблем)

---

## Швидкий старт

Для досвідчених користувачів:

```bash
cd C:\MyFolder\IvolTutor
docker compose down
docker compose up --build
```

Відкрийте http://localhost:5173 та зареєструйтеся.

**Детальна інструкція нижче ↓**

---

## Покрокова інструкція запуску

### Крок 1: Відкрийте PowerShell

Натисніть `Windows + X` → виберіть "Windows PowerShell" або пошукайте "PowerShell" в меню Start

### Крок 2: Перейдіть в папку проєкту

```powershell
cd C:\MyFolder\IvolTutor
```

### Крок 3: Перевірте Docker

```powershell
docker --version
```

Має бути: `Docker version 27.0.0, build ...`

❌ Якщо помилка → встановіть [Docker Desktop](https://www.docker.com/products/docker-desktop)

### Крок 4: Зупиніть старі контейнери (якщо є)

```powershell
docker compose down
```

### Крок 5: Запустіть контейнери

```powershell
docker compose up --build
```

> Якщо `docker compose` не працює, спробуйте `docker-compose`

⏳ **Очікуйте 2-5 хвилин**

Ви побачите багато логів. Очікуйте поки з'явиться:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Крок 6: Відкрийте додаток

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Документація**: http://localhost:8000/docs

---

## Тестування автентифікації

### Реєстрація нового користувача

1. Відкрийте http://localhost:5173
2. Натисніть **"Зареєструватися"**
3. Заповніть форму:
   - **Email**: test@example.com
   - **Ім'я**: Test User
   - **Пароль**: password123
   - **Підтвердження**: password123
4. Натисніть **"Зареєструватися"**
5. Ви будете перенаправлені на календар ✅

### Вхід в систему

1. Натисніть на **аватар** у правому верхньому куті
2. Натисніть **"Вихід"**
3. На Welcome page натисніть **"Увійти"**
4. Введіть email та пароль
5. Натисніть **"Увійти"**

### Перевірка токена

1. Натисніть **F12** (DevTools)
2. Перейдіть: **Application** → **LocalStorage** → **http://localhost:5173**
3. Ви маєте бачити:
   - `access_token` - JWT токен
   - `user` - дані користувача

### Перевірка Network запитів

1. **F12** → вкладка **Network**
2. Оновіть сторінку або виконайте дію (напр., додайте учня)
3. Виберіть будь-який запит до `/students/` або `/lessons/`
4. Перегляньте **Headers** → має бути:
   ```
   Authorization: Bearer eyJhbGci...
   ```

---

## API Endpoints

### Публічні (без токена)

```
POST /auth/signup    - Реєстрація
POST /auth/login     - Вхід
```

### Захищені (потрібен Bearer токен)

```
GET    /auth/me          - Дані поточного користувача
GET    /students/        - Список учнів
POST   /students/        - Додати учня
PATCH  /students/{id}    - Оновити учня
GET    /students/{slug}  - Отримати учня за slug
GET    /lessons/         - Список занять
POST   /lessons/         - Додати заняття
PATCH  /lessons/{id}     - Оновити заняття
POST   /payments/        - Додати платіж
GET    /payments/        - Список платежів
POST   /upload/          - Завантажити файли
```

**Повна документація**: [backend/AUTH_API.md](../backend/AUTH_API.md)

---

## Налаштування

### Backend Environment

Створіть `.env` в папці `backend/`:

```env
SECRET_KEY=your-secret-key-change-in-production
DATABASE_URL=sqlite:///./ivol_tutor.db
```

> SECRET_KEY вже налаштований в `docker-compose.yml`

### Frontend Environment

Файл `frontend/.env.local` (вже створений):

```env
VITE_API_URL=http://localhost:8000
```

### Зміна порту Frontend

В `docker-compose.yml`:
```yaml
frontend:
  ports:
    - "3000:5173"  # Змініть 5173 на інший порт
```

### Зміна порту Backend

В `docker-compose.yml`:
```yaml
backend:
  ports:
    - "9000:8000"  # Змініть 8000 на інший порт
```

Також оновіть `frontend/.env.local`:
```env
VITE_API_URL=http://localhost:9000
```

---

## Вирішення проблем

### ❌ "Connection refused"

**Причина**: Контейнери не запущені

**Рішення**:
```powershell
docker ps  # Перевірте запущені контейнери
docker compose up --build  # Перезапустіть
```

### ❌ Frontend не завантажується

**Причина**: Frontend контейнер ще встановлює залежності

**Рішення**:
- Зачекайте 30-60 секунд
- Оновіть сторінку (F5)
- Перевірте логи: `docker logs ivoltutor-frontend-1`

### ❌ CORS помилка

**Причина**: Backend не дозволяє запити з Frontend

**Рішення**:
1. Перевірте `backend/src/main.py` - має бути:
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["http://localhost:5173"],
       ...
   )
   ```
2. Перезапустіть: `docker compose restart backend`

### ❌ 401 Unauthorized

**Причина**: Токен відсутній, неправильний або протермінований

**Рішення**:
1. Очистіть localStorage (F12 → Application → LocalStorage → Clear)
2. Вийдіть і увійдіть знову
3. Перевірте термін дії токена (7 днів)

### ❌ "User already exists"

**Причина**: Email вже зареєстровано

**Рішення**:
- Використайте інший email
- Або спробуйте увійти з існуючим

### ❌ Docker команди не працюють

**Причина**: Docker Desktop не запущений

**Рішення**:
1. Запустіть Docker Desktop
2. Зачекайте поки він повністю завантажиться
3. Спробуйте команди знову

### ❌ Порт вже використовується

**Помилка**: `bind: address already in use`

**Рішення**:
```powershell
# Знайдіть процес на порту 5173 або 8000
netstat -ano | findstr :5173
netstat -ano | findstr :8000

# Зупиніть процес (замініть PID на номер з команди вище)
taskkill /PID <PID> /F
```

### ❌ PostgreSQL помилки

**Рішення**:
```powershell
# Видаліть volumes та перезапустіть
docker compose down -v
docker compose up --build
```

### 📋 Перегляд логів

```powershell
# Всі логи
docker compose logs -f

# Тільки backend
docker logs ivoltutor-backend-1 -f

# Тільки frontend
docker logs ivoltutor-frontend-1 -f

# Тільки база даних
docker logs ivoltutor-db-1 -f
```

---

## Корисні команди

### Зупинка

```powershell
docker compose down
```

### Перезапуск

```powershell
docker compose restart
```

### Перезапуск одного сервісу

```powershell
docker compose restart backend
docker compose restart frontend
```

### Видалення всього (включно з БД)

```powershell
docker compose down -v
```

> **Увага**: Це видалить всі дані з бази даних!

### Запуск в фоновому режимі

```powershell
docker compose up -d
```

### Вхід в контейнер

```powershell
# Backend
docker exec -it ivoltutor-backend-1 bash

# Frontend
docker exec -it ivoltutor-frontend-1 sh
```

---

## Локальна розробка (без Docker)

### Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8000
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

**База даних**: Використовується SQLite (`backend/ivol_tutor.db`)

---

## Структура JWT токена

Токен складається з трьох частин:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ0ZXN0QGV4YW1wbGUuY29tIiwiZXhwIjoxNzA2ODQwMDAwfQ.xxx
├─ Header (алгоритм HS256)
├─ Payload (email + час закінчення)
└─ Signature (підпис)
```

**Термін дійсності**: 7 днів

**Декодувати**: https://jwt.io

---

## Що далі?

✅ Автентифікація налаштована  
✅ Всі endpoints захищені  
✅ Frontend інтегрований  

Тепер ви можете:
- Додавати нових учнів
- Планувати заняття
- Вести облік платежів
- Завантажувати матеріали

**Успішної роботи! 🚀**
